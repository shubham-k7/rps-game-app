import { GameMode } from "../types/game"

export const GameModeConstants = {
    PVP: 'PlayerVPlayer' as GameMode,
    PVC: 'PlayerVComputer' as GameMode
}

export const GameModeLabels = {
    'PlayerVPlayer': 'Player vs Player',
    'PlayerVComputer': 'Player vs Computer'
}